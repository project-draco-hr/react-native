{
  MemoryPressureListener[] listeners=mListeners.toArray(new MemoryPressureListener[mListeners.size()]);
  for (  MemoryPressureListener listener : listeners) {
    listener.handleMemoryPressure(level);
  }
}
