{
  if (name == null || name.isEmpty()) {
    return null;
  }
  int resId=ResourceDrawableIdHelper.getInstance().getResourceDrawableId(context,name);
  return new Uri.Builder().scheme(ContentResolver.SCHEME_ANDROID_RESOURCE).authority(context.getPackageName()).path(String.valueOf(resId)).build();
}
