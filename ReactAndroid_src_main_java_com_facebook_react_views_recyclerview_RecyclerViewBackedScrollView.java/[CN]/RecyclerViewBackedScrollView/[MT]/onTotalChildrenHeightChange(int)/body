{
  if (mSendContentSizeChangeEvents) {
    ((ReactContext)getContext()).getNativeModule(UIManagerModule.class).getEventDispatcher().dispatchEvent(new ContentSizeChangeEvent(getId(),SystemClock.elapsedRealtime(),getWidth(),newTotalChildrenHeight));
  }
}
