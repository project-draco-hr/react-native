{
  ReactModule module=getClass().getAnnotation(ReactModule.class);
  if (module == null) {
    FLog.w(ReactConstants.TAG,"Module " + getName() + " lacks @ReactModule annotation, assuming false for supportsWebWorkers()");
    return false;
  }
  return module.supportsWebWorkers();
}
